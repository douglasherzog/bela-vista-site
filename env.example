# Copie este arquivo para .env (opcional) ou configure as variáveis no seu terminal
#
# SQLite local (padrão do projeto):
# DATABASE_URL=sqlite:///./belavista.db
#
# Postgres local (exemplo):
# DATABASE_URL=postgresql+psycopg://postgres:postgres@localhost:5432/belavista

DATABASE_URL=sqlite:///./belavista.db
ADMIN_USER=admin
ADMIN_PASS=senha
